[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "findicle"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "beautifulsoup4",
    "openpyxl",
    "pyyaml",
    "python-dotenv",
]
